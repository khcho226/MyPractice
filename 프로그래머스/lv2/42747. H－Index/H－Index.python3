def solution(citations):
    citations.sort()
    ans = 0
    
    for i in range(len(citations)):
        if ans <= len(citations) - i:
            ans = min(citations[i], len(citations) - i)
    
    return ans