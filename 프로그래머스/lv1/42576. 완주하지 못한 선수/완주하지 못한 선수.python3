def solution(participant, completion):
    dic = {}

    for i in participant:
        if i not in dic:
            dic[i] = 1
        else:
            dic[i] += 1
            
    for j in completion:
        dic[j] -= 1
        if dic[j] == 0:
            del dic[j]

    return list(dic.keys())[0]