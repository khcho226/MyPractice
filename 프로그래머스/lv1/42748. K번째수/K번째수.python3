import heapq

def solution(array, commands):
    answer = []
    
    for i in commands:
        arr = []
        for j in range(i[1] - i[0] + 1):
            heapq.heappush(arr, array[i[0] - 1 + j])
        arr.sort()
        answer.append(arr[i[2] - 1])
    
    return answer