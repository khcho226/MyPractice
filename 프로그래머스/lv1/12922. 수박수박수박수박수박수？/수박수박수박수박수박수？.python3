def solution(n):
    answer = ""
    cnt = 1
    
    for i in range(n):
        if cnt == 1:
            answer += "수"
        else:
            answer += "박"
            
        cnt = 1 - cnt
        
    return answer